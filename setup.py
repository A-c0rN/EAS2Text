from setuptools import find_packages, setup

setup(
    name='EAS2Text',
    packages=find_packages(include=['EAS2Text']),
    version='0.1.0',
    description='A Python library to convert raw EAS header data to a human readable text',
    author='A-c0rN',
    license='ODbL-1.0',
    install_requires=[],
)

